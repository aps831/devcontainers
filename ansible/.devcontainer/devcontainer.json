{
    "name": "Ansible",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {}
    },
    "settings": {
        "files.associations": {
            "**/*.yaml": "ansible",
            "**/*.yml": "ansible"
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/github-cli:1": {}
    }
}
